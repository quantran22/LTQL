@model IEnumerable<DemoLTQL.Models.Khoa>

@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.KhoaName)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.KhoaName)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.KhoaID }) |
            @Html.ActionLink("Details", "Details", new { id=item.KhoaID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.KhoaID })
        </td>
    </tr>
}

</table>
